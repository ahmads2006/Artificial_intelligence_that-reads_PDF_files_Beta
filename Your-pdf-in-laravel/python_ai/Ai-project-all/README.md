# 🤖 نظام الإجابة الذكي على الأسئلة من ملفات PDF

نظام متقدم للإجابة على الأسئلة من ملفات PDF باستخدام الذكاء الاصطناعي من Google Gemini.

## 🚀 الميزات

- ✅ **تشغيل تلقائي**: يبدأ النظام تلقائياً مع API key مُعد مسبقاً
- ✅ **تحميل تلقائي**: يحمل ملف PDF افتراضي عند البدء
- ✅ **ذكاء اصطناعي**: إجابات ذكية باستخدام Google Gemini
- ✅ **بحث تقليدي**: بديل موثوق في حالة فشل AI
- ✅ **واجهة تفاعلية**: سهلة الاستخدام باللغة العربية والإنجليزية
- ✅ **بحث متقدم**: إمكانية البحث عن كلمات مفتاحية
- ✅ **تلخيص ذكي**: تلخيص المستندات باستخدام AI

## 📋 الأوامر المتاحة

- **اكتب سؤالك مباشرة**: للحصول على إجابة ذكية
- `search:كلمة` - للبحث عن كلمة معينة
- `summary` - للحصول على ملخص ذكي
- `toggle` - للتبديل بين AI والبحث التقليدي
- `load` - لتحميل ملف PDF جديد
- `help` - عرض المساعدة
- `exit` - للخروج

## 🛠️ المتطلبات

1-Python

الإصدار: 3.8 أو أعلى

2-المكتبات المطلوبة

جميع مكتبات Python المطلوبة يتم تثبيتها تلقائياً داخل البيئة الافتراضية venv

المكتبات الأساسية المستخدمة في المشروع:

PyPDF2

python-dotenv

google-generativeai

3-مفتاح API لـ Gemini

تحتاج إلى API Key خاص بك من Gemini

ضع المفتاح داخل ملف .env في نفس مجلد المشروع

مثال لملف .env:

GEMINI_API_KEY=your_api_key_here


النموذج المستخدم: gemini-flash-latest

## 🚀 كيفية التشغيل

1. تأكد من تفعيل البيئة الافتراضية:
```bash
venv\Scripts\activate  # على Windows
```

2. شغل النظام:
```bash
python main.py
```

3. النظام سيتحمل تلقائياً ملف `data/UML.pdf` ويبدأ الوضع التفاعلي

## 📁 هيكل المشروع

```
RGPAI_project/
├── main.py              # الملف الرئيسي للنظام
├── test_models.py       # اختبار نماذج Gemini
├── requirements.txt     # المتطلبات
├── README.md           # هذا الملف
├── data/
│   ├── UML.pdf         # ملف PDF تجريبي
│   └── rules.pdf       # ملف PDF إضافي
└── venv/               # البيئة الافتراضية
```

## 🔧 التخصيص

### تغيير API Key
في ملف `main.py`، غيّر السطر:
```python
api_key = "#############"
```

### تغيير ملف PDF الافتراضي
غيّر المسار في:
```python
default_pdf = "data/UML.pdf"
```

### تغيير النموذج
في دالة `_initialize_ai`:
```python
self.ai_model = genai.GenerativeModel('gemini-flash-latest')
```

## 📊 الميزات التقنية

- **معالجة PDF**: استخراج النص من ملفات PDF
- **تقسيم النصوص**: معالجة المستندات الكبيرة
- **بحث ذكي**: استخدام Gemini للإجابات الدقيقة
- **واجهة متعددة اللغات**: دعم العربية والإنجليزية

## 🎯 أمثلة على الاستخدام

```
💬 Enter command or question: What are the main concepts in UML?

🤔 Searching for answer...
✨ Answer:
UML (Unified Modeling Language) is a standard way to visualize the design of a system...

💬 Enter command or question: search:class

🔍 Found 5 results:
  • [Line 45]: A class is a blueprint for creating objects...

💬 Enter command or question: summary

📝 Generating summary...
📄 Summary:
This document covers UML basics including classes, objects, inheritance...
```

## 🔒 الخصوصية والأمان

- API key مخزن محلياً في الكود
- لا يتم إرسال البيانات إلى خوادم خارجية إلا لـ Google Gemini
- جميع المعالجة تتم محلياً

## 📞 الدعم

لأي مشاكل أو اقتراحات، يرجى مراجعة الكود أو تعديله حسب الحاجة.
